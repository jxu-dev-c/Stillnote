# MOSS custom code requires Transformers 5 in the isolated inference runtime.
transformers>=5,<6
numpy>=1.26,<3

# Native Apple GPU runtime; same verified MOSS checkpoint, no alternate model download.
# The Swift app decodes audio and parses transcripts, so the worker needs neither
# PyAV/librosa for decoding nor PyTorch for a fallback backend.
mlx-audio @ git+https://github.com/Blaizzy/mlx-audio.git@17001a6950956302f15b53d86b601324efe716ba
